pipeline{
    agent any
        stages{
            stage("Build"){
                steps{
                    script{
                        def name = "DevOps"
                        if (name == "DevOps")
                            println ("Hi ${name}")
                        else
                            println ("Hi Human")
                        sleep 2
                        echo "end demo now but It will be continue "
                        
                    }
                }
            }
            
        }
}
